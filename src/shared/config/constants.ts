/**
 * 테이블 위젯 페이지 크기.
 *
 * 위젯마다 흩어져 있던 매직넘버를 세 단계로 수렴시킨다.
 * 새 테이블 위젯은 반드시 이 중 하나를 쓴다.
 */
export const TABLE_PAGE_SIZE = {
  /** 한 화면에 여러 패널이 나란히 오는 좁은 테이블 */
  COMPACT: 5,
  /** 페이지 본문을 차지하는 일반 테이블 */
  DEFAULT: 10,
  /** 행이 짧고 목록이 긴 테이블 */
  WIDE: 20,
} as const;

/**
 * 사용자에게 보여줄 표준 에러 문구.
 *
 * 같은 상황에 서로 다른 문구가 쓰이던 것을 통일한다.
 * 도메인별로 더 구체적인 문구가 필요하면 그 자리에서 별도 문자열을 쓴다.
 */
export const ERROR_MESSAGE = {
  /** 조회 실패 */
  FETCH: '데이터를 불러오지 못했습니다.',
  /** 네트워크·서버 연결 실패 */
  NETWORK: '서버 연결에 실패했습니다.',
  CREATE: '등록에 실패했습니다.',
  UPDATE: '수정에 실패했습니다.',
  DELETE: '삭제에 실패했습니다.',
} as const;
